<!-- Home Page Layout
  –––––––––––––––––––––––––––––––––––––––––––––––––– -->
<div class="container">
    <div class="header">
        <h4>WAITER AVAILABILITY</h4>
    </div>
    {{!-- <div class="week_days">
        <h5>WELCOME</h5>
        <h6 style="margin-left: 10px;">WELCOME <span style="font-size: 30px; color:  #5499c7">{{names}} </span> BOOK
            YOUR
            SHIFTS FOR THE WEEK..!</h6>
        <br>
    </div> --}}
    <div class="enterDates">
        <div class="week_days">
            <h5>WELCOME</h5>
            <h6 style="margin-left: 10px;">WELCOME <span style="font-size: 30px; color:  #5499c7"> {{names}} </span>
                BOOK YOUR
                SHIFTS FOR THE WEEK..!</h6>
            <br>
        </div>
        <form class="" action="/waiters/{{names}} " method="post">
            <div class="boxes">
                <h5 style="text-align: center;">PLEASE SELECT WORKING DAYS..!</h5>
                {{#each displayDays}}
                <h6>
                    <input type="checkbox" id="days"  value="{{weekday}}" {{checked}}>
                    <label for="days"> {{weekday}} </label>
                </h6>
                {{/each}}
            </div>
            <button type="submit" class="button-primary">SUBMIT SHIFTS</button>
        </form>
    </div>
</div>

<!-- End Document
  –––––––––––––––––––––––––––––––––––––––––––––––––– -->